可实现功能：
	主界面
		实现代码运行
		文件读取及保存
		组件随窗体可变化
		实现关键字的提示与tab键进行补全
	输入界面
		实现输入数据并返回数据

	基本数据类型
		int / float / string / bool
	变量
		赋值及使用
	全局数组 int a[] / float a[] / string a[] / bool a[]
		定义及赋值
	语句结束符
		$

	整数与浮点数 加+ /减- /乘* /除/ /取模% /乘方**
		精度最多保留5位小数
	布尔值
		逻辑运算 & / |
	字符串 乘法 *
		重复输出与乘数相同的字符串个数，不可反转乘数与被乘数
	字符串 相加 str + str / int / float / bool
		字符串可与其他类型数据进行拼接
	字符串与数值 
		比较运算 > / >= / < / <= / == 

	条件语句
		if 
		if else
		while
		continue
		break
	
	已定义的函数
		输出函数
			print()
		输入函数
			readInt()
			readFloat()
			readString()
			readBool()
		自定义函数及调用
	